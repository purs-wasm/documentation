{
  "name": "web",
  "type": "module",
  "scripts": {
    "compile": "cd .. && spago build -p web",
    "test": "spago test",
    "format": "purs-tidy format-in-place src",
    "check": "purs-tidy check src",
    "sync": "bash ../scripts/sync-docs.sh",
    "dev": "pnpm run sync && spago build && vite",
    "build": "spago build && vite build"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "marked-gfm-heading-id": "^4.1.4",
    "minisearch": "^7.2.0",
    "shiki": "^4.2.0"
  }
}